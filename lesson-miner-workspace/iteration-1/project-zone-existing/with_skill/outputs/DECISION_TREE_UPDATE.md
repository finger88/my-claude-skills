# 决策树更新记录

## 更新前（2个卡点）

```text
看到 API 连接错误？
  ├─ 是 → 检查飞书 webhook URL 和 token 是否过期
  └─ 否 → 继续判断...

看到食材识别不准确？
  ├─ 是 → 检查图片清晰度和光线条件
  └─ 否 → 继续判断...
```

## 更新后（3个卡点）

```text
看到 API 连接错误？
  ├─ 是 → 检查飞书 webhook URL 和 token 是否过期
  └─ 否 → 继续判断...

看到食材识别不准确？
  ├─ 是 → 检查图片清晰度和光线条件
  └─ 否 → 继续判断...

看到 API 返回数据解析错误？
  ├─ 是 → 检查字段类型是否与文档一致（特别是数组 vs 字符串）
  └─ 否 → 继续判断...
```

## 新增节点说明

- **触发条件**: 解析 API 响应时出现类型错误
- **判断逻辑**: 检查字段实际类型与文档声明是否一致
- **解决方案**: 添加类型检查和容错处理（字符串需反序列化）
